# playbooks/

按 8 个评估维度组织的可执行剧本（facade / docs / community / release / quality / growth / seo / case-library）。

每份 playbook 必须有 YAML front-matter：`id, dimension, version, status, last_verified_at`，以便脚本索引、评分卡引用和案例库交叉链接。
